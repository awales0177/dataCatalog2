// Model fields configuration
// This file can be edited as YAML and converted, or edited directly as JS
// Format: field1 at top level with name, jsonKey, and options (empty array for free text)

export const modelFieldsConfig = {
  field1: {
    name: "Sensitivity Level",
    jsonKey: "sensitivityLevel",
    options: [] // Empty array means free text input
  }
};